#include <stdio.h>
int main() {
    int a1[] = {1, 3, 5, 7};
    int a2[] = {2, 4, 6, 8};
    int n1 =4;
    int n2 = 4;
    int k = n1 + n2;
    int a[k];
    for (int i = 0; i < n1; i++)
        a[i] = a1[i];
    for (int i = 0; i < n2; i++)
        a[n1 + i] = a2[i];
    for (int i = 0; i <k; i++) 
        printf("%d ", a[i]);
    printf("\n");
    return 0;
}
